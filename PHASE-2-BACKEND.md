# 🚀 خطة المرحلة الثانية: ربط الخوادم وقواعد البيانات (Phase 2 Backend Architecture)

## 1. ربط Gmail و Outlook عبر OAuth 2.0:
- استخدام `passport-google-oauth20` و `passport-azure-ad` لإجراء عملية المصادقة دون حفظ كلمات المرور.
- إرسال الرسائل المنمقة بـ HTML عبر البريد الإلكتروني الرسمي للمستخدم مباشرة بعد موافقته الصريحة.

## 2. محرك قراءة الصور والـ PDF (OCR Engine):
- دمج مكتبة `Tesseract.js` لمعالجة لقطات الشاشة وصور إعلانات التوظيف واستخراج النصوص باللغتين العربية والإنجليزية.

## 3. التكامل مع PostgreSQL و Prisma ORM:
- مزامنة كافة الطلبات المستخرجة وسجلات الأمان والـ Audit Logs مع قاعدة البيانات المشفرة بـ AES-256.
